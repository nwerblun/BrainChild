using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Add Require component for player info which will have HP, etc.
public class AffectedByProjectiles : MonoBehaviour
{
    //Have a function to call on hit animation
    //Have a function to change HP
    //Have a function to be knocked back
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
